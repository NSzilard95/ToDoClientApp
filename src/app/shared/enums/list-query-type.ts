/**
 * Enum for determine the list query type
 */
export enum ListQueryType {
  /**
   * Query all todos
   */
  All = 0,
  /**
   * Query in progress todos
   */
  InProgress = 1,
  /**
   * Query done todos
   */
  Done = 2
}
